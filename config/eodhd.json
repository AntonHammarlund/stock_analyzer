{
  "enabled": false,
  "api_token_env": "EODHD_API_TOKEN",
  "api_token": "",
  "timeout_sec": 30,
  "exchanges": [
    {
      "code": "US",
      "asset_type": "stock"
    }
  ],
  "notes": "Disabled to keep the workflow free. Enable only if you have a paid EODHD plan."
}
